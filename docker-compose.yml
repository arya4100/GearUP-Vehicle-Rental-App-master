version: '3.8'

services:
  react-app:
    build: .
    volumes:
      - ./final-demo:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true # Useful for file watching on Windows/Docker
    stdin_open: true
    tty: true
